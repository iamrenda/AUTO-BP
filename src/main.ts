import "./eventListener/eventListener";

//////////////////////////////////////
//////////////////////////////////////
//////////////////////////////////////
// import * as mc from "@minecraft/server";
// import { giveItems } from "./utilities/utilities";
// import { DynamicProperty } from "./data/dynamicProperty";

// mc.world.afterEvents.chatSend.subscribe(() => {
//   DynamicProperty.resetDynamicData();
//   mc.world.sendMessage(`${mc.world.getDynamicProperty("auto:dynamicData")}`);
//   mc.world.sendMessage(`${mc.world.getDynamicProperty("auto:storedBlocks")}`);
//   mc.world.sendMessage(`${mc.world.getDynamicProperty("auto:gameData")}`);
//     giveItems("Bridger");
// });
